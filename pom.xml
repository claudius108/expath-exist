<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>ro.kuberam</groupId>
		<artifactId>models</artifactId>
		<version>0.0.1-SNAPSHOT</version>
	</parent>

	<groupId>org.expath.exist</groupId>
	<artifactId>expath-exist</artifactId>
	<packaging>pom</packaging>
	<version>1.0-SNAPSHOT</version>
	<name>eXist wrappers for EXPath modules</name>
	<url>http://expath.org</url>

	<modules>
		<module>expath-exist-crypto</module>
		<module>expath-exist-ft-client</module>
	</modules>

	<dependencies>
		<dependency>
			<groupId>log4j</groupId>
			<artifactId>log4j</artifactId>
			<version>1.2.16</version>
		</dependency>
		<dependency>
			<groupId>commons-pool</groupId>
			<artifactId>commons-pool</artifactId>
			<version>1.5.6</version>
		</dependency>
		<dependency>
			<groupId>org.codehaus.fabric3.fabric3-db-exist</groupId>
			<artifactId>xmldb</artifactId>
			<version>1.0</version>
		</dependency>
		<dependency>
			<groupId>commons-io</groupId>
			<artifactId>commons-io</artifactId>
			<version>2.1</version>
		</dependency>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.8.1</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.exist-db</groupId>
			<artifactId>exist-modules</artifactId>
			<version>2.0-tech-preview</version>
		</dependency>
		<dependency>
			<groupId>org.exist-db</groupId>
			<artifactId>exist</artifactId>
			<version>2.0-tech-preview</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>xml-maven-plugin</artifactId>
				<version>1.0</version>
				<executions>
					<execution>
						<id>generate-xar-descriptors</id>
						<phase>prepare-package</phase>
						<goals>
							<goal>transform</goal>
						</goals>
						<configuration>
							<forceCreation>true</forceCreation>
							<transformationSets>
								<transformationSet>
									<dir>../../expath-specs/${expath-spec-id}</dir>
									<includes>
										<include>${expath-spec-id}.xml</include>
									</includes>
									<stylesheet>../utils/generate-xar-descriptors.xsl</stylesheet>
									<parameters>
										<parameter>
											<name>package-version</name>
											<value>${project.version}</value>
										</parameter>
										<parameter>
											<name>target-dir</name>
											<value>target/files</value>
										</parameter>										
										
										<parameter>
											<name>package-main-class</name>
											<value>${package-main-class}</value>
										</parameter>
									</parameters>
								</transformationSet>
							</transformationSets>
						</configuration>
					</execution>
				</executions>
				<dependencies>
					<dependency>
						<groupId>net.sf.saxon</groupId>
						<artifactId>Saxon-HE</artifactId>
						<version>9.4</version>
					</dependency>
				</dependencies>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<id>copy-dependencies</id>
						<phase>package</phase>
						<goals>
							<goal>copy-dependencies</goal>
						</goals>
						<configuration>
							<outputDirectory>${project.build.directory}/files/${project.artifactId}/</outputDirectory>
							<overWriteReleases>false</overWriteReleases>
							<overWriteSnapshots>false</overWriteSnapshots>
							<overWriteIfNewer>true</overWriteIfNewer>
							<excludeTransitive>true</excludeTransitive>
							<includeArtifactIds>${dependency-list}</includeArtifactIds>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<version>1.7</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<target name="copy-module-jar" description="Copy module jar">
								<echo>Copy ${project.artifactId}-${project.version}.jar...</echo>
								<copy file="${project.build.directory}/${project.artifactId}-${project.version}.jar" todir="${project.build.directory}/files/${project.artifactId}/"
									overwrite="true" />
								<!-- </target> -->
								<!-- <target name="deploy-module-to-local-eXist-server" description="Deploy module jars and its dependencies to local 
									eXist server"> -->
								<!-- <echo>Deploy module jar and its dependencies to local eXist server...</echo> -->
								<!-- <copy todir="/home/claudius/eXist/webapp/WEB-INF/expathrepo/${ant.project.name}-${module-version}" -->
								<!-- overwrite="true"> -->
								<!-- <fileset dir="${kuberam.temp.dir}/files"> -->
								<!-- <include name="**/*.*" /> -->
								<!-- </fileset> -->
								<!-- </copy> -->
								<!-- </target> -->
								<!-- <target name="generate-xar" description="Generate xar archive"> -->
								<echo>Making ${project.artifactId}-${project.version}.xar...</echo>
								<!-- <xslt force="true" style="${kuberam.web.specs.dir}/utils/generate-module/generate-xar-descriptors.xsl" in="${kuberam.web.specs.dir}/${ant.project.name}/${ant.project.name}.xml" -->
								<!-- out="${kuberam.temp.dir}/files/a.xml"> -->
								<!-- <param name="module-version" expression="${module-version}" /> -->
								<!-- <param name="eXist-main-class-name" expression="${eXist-main-class-name}" /> -->
								<!-- </xslt> -->
								<!-- <delete file="${kuberam.temp.dir}/files/a.xml" /> -->

								<zip destfile="${project.build.directory}/${project.artifactId}-${project.version}.xar">
									<fileset dir="${project.build.directory}/files">
										<include name="**/*.*" />
										<exclude name="*-tests.jar" />
									</fileset>
								</zip>
							</target>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>